import { PRIMARY_COLOR, PRIMARY_COLOR_DARK } from "./colors";

export const MuiTableCell = {
    styleOverrides: {
        root: {color:PRIMARY_COLOR, borderBottom:`1px solid ${PRIMARY_COLOR_DARK}`},
    }
}